const express = require('express');
const router = express.Router();
const {home, searchBooks} = require('../controllers/books.controller');

// router.get('/home', home);
// router.get('/books/:', searchBooks);



// module.exports = router;